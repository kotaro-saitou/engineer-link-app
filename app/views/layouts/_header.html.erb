<div class="header">
  <div class="container">
      <div class="row">
          <div class="col-xs-12">
            <h1 class='text-center Engineer-link' style='color:#FFFFFF'>Engineer Link</h1>
          </div>
      </div>
  </div>
</div>

<% if logged_in? %>
  <%= render 'layouts/navbar_login' %>
<% else %>
  <%= render 'layouts/navbar' %>
<% end %>
      